## Design decision

- Original file is currently store at disk; In the future, it should be stored at object storage (s3) during conversion for scalability and robustness and only publish the file metadata